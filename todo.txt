get back to optimizing away

familyValues

per attribute rules, eg, enabled on/off

check we can add kid in middle

have observer on kids call not-to-be on lostks

ajax demo

create a chunk of dom in a separate function and pull into parent

why does clear not work if cycle detected? (move button lower in jWebTest)